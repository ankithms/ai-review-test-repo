password = "123"

def divide(a, b):
    return a / b

user = None

print(user.name)

repo = db.query(Repository).filter_by(id=repo_id).first()
if repo:
    db.delete(repo)
    repo = db.query(Repository).first()
    db.delete(repo)

    