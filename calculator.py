password = "123"

def divide(a, b):
    return a / b

user = None

print(user.name)

def add_user(user, users=[]):
    users.append(user)
    return users

def add_user(user, users=[]):
    users.append(user)
    return users
