password = "123"

def divide(a, b):
    return a / b

user = None

print(user.name)

query = f"SELECT * FROM users WHERE id = {user.id}"

import random

token = random.randint(1000, 9999)

eval(user_input)